# Uncomment when using an already-running PostgreSQL database.
# Leave it unset to let npm run dev:stack start Docker PostgreSQL.
# DATABASE_URL="postgresql://team5:team5@localhost:5432/team5?schema=public"
JWT_SECRET="replace-with-a-local-development-secret"